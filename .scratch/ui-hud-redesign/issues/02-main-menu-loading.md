# 02 — 主菜单、加载与入场演出

**What to build:** 重做主菜单为完整的东方式标题画面，保留现有菜单编排。左上为艺术主标题 **棍維何意味**，其下小型英文副标题 **OTTOWiki Project**；左下为十项直达导航，顺序 Start Game、Extra Start、Stage Select、Practice、Ranking、Replay、Manual、History、Settings、Exit，按间距与层级分组，不新增子菜单。右侧为饮泉思源单人主体（结构阶段可占位）。视觉以浅纸水墨为基线，借 th14 非对称标题/人物/菜单构图与纹样拼贴，以 th18 多色碰撞、装饰叠层与展示字形加强，用纸墨远近与笔刷层做出比第三版更迷幻、华饰的层次，不得收敛回极简米色网页。艺术主标题保持已定汉字，导航短词英文主、中文辅；装饰、人物与墨层不拦截点击。

**Blocked by:** None — can start immediately

**Status:** implemented — 本票功能与结构验收通过；人物及最终美术独立审核

- [x] 正式浏览器启动后标题「棍維何意味」与副标题 OTTOWiki Project 正确呈现，十项入口全部可见且逐项可达，点击/按键能进入对应页面，无新增代码错误。
- [x] 首次进入主菜单播放完整分层绘画入场演出；演出任何阶段按确认键或点击立即跳到稳定完成态，且该次跳过输入被消费，不额外激活当前焦点选项。
- [x] 从任一子页面返回主菜单时重播完整入场演出；返回后焦点保留上次选择项，选项切换与执行不再触发主菜单入场演出。
- [x] 键盘方向键/WASD 单次移动只改变焦点，Enter/Z/Space 确认，Esc/X 返回；鼠标命中区域与可见控件一致，点击不会穿透到装饰层。
- [x] 触屏点击与确认路径清晰，单次输入不会误触发下一状态；演出期间第一次点击只跳过。普通主菜单入口沿用已确认的直达操作，不把路线页的“先选中再确认”扩大为全部菜单必须点两次。移动竖屏独立排版，标题、导航、返回与角色主体不被裁切或遮挡。
- [x] reduced-motion 开启时直接进入稳定呈现态，不播放装饰性移动，且所有信息与导航保持可用。
- [x] 真实等待资源才显示 Loading；进度只在可真实测量时显示。加载失败给出可理解反馈与既有恢复路径，不显示假进度条、不永远停留在假进度画面。
- [x] Exit 复用当前平台能兑现的退出/返回行为，给出清晰反馈，不留下无效按钮、不强行关闭浏览器。
- [x] 主标题优先日式汉字字形，台湾繁体仅作标题缺字兜底；导航与HUD短词以外的中文不全局英文化或转繁体。与参考截图并排检查游戏标题画面构图、选中态和装饰层次；该视觉认可不代表最终人物素材已通过。

**范围与协作：** 本票只做主菜单呈现、加载反馈与入场/跳过演出；难度、自机、配置、选关、练习等下游页面由各自票负责，不因本票阻塞他人。人物立绘为占位，最终立绘由后置资产清单与统一审核承担（18）。

**规格覆盖：** ID-02、ID-04（主菜单与演出）、ID-06（输入优先级）、ID-08、ID-09（加载）；US1–US13、US64、US68；T01、T02、T18、T19、T20（本票涉及部分）。

## Comments

2026-09-10：已接入正式游戏，分支 `feat/main-menu-entrance`。主菜单、真实资源加载、可跳过/可重播入场、十项直达及焦点记忆已实现；Exit 提供可兑现的标签页退出提示与返回。人物仍为明确占位，未生成立绘，下游配置/模式未扩展。

验证：`npm test` 119/119；`npm run test:e2e` 10/10。正式浏览器覆盖 1365×900、1280×800、390×844、320×568，十项入口与返回、键鼠/触控单次输入、按住确认/返回、减少动态效果均通过；320px 以区域滚动保留 44px 命中区。真实 Start Game→第一章→暂停/恢复通过，版面仍为450×600，无新增页面异常。标题实测使用 Yu Mincho Demibold 五个汉字。

加载故障覆盖模块失败及重载、图片失败后继续、慢资源、Three 全镜像失败后的简化背景，以及320×568恢复按钮/按住Enter交接。错误/恢复状态不继续显示Loading。审查修复键位捕获取消穿透、按住返回重复消费、过期指针标志吞键盘输入、加载决定态提示与窄屏恢复可达性；删除无消费者的报告字段及样式。

证据：`../evidence/02-main-menu/` 的 `smoke-results.json`、`fault-results.json`、`story-font-results.json` 与 desktop/mobile/loading/recovery/entrance/story-pause 截图。两轮正式渲染已检查；视觉采用左上题字、左下文字导航、纸墨洗色/纹样与右侧明确占位。此记录不将占位或前端结构通过宣称为最终美术通过。

2026-09-10 视觉修订：按用户反馈将主菜单及加载页五字拆分排版，「棍維」朱红／金边，其余冷墨蓝／浅边，字级和基线错落。导航改为原作风格粗衬线描边、错位分组。矮桌面窗口与手机采用独立排版／区域滚动，保留既有输入和十项入口。候选魔幻背景仅作为会话预览，未接入游戏；`index.html`、资源预载和授权清单均未保留不存在的 `menu-dream.avif` 引用。

本轮实际验证：`npm test` 119/119、`npm run test:e2e` 10/10（进程退出码0）；当前HTML/CSS的烟测覆盖1365×900、1280×800、1264×625、390×844、320×568标题几何/异色/十项入口，以及桌面/手机图片失败继续、模块失败重载、慢资源、Three全镜像失败继续，共10场景，最终退出码0。四档尺寸的菜单检查曾遇现有图像缓存准备超时，记录原因并经真实“继续进入”后通过，不把降级启动冒充全部资源成功。

本轮证据仅本地：`../evidence/02-title-revision/results.json`（含被测文件SHA-256）及menu/recovery/module-failure截图，未纳入Git提交，远程审阅需另行附上。定向代码审查未发现P1/P2；候选图及人物均不代表最终美术通过。

2026-09-10 PR #32 CI 修订：远程 run `34489686024` 的 10 个 E2E 全部在 `waitForGameReady` 超时；失败页面显示两项音乐加载超时及真实「继续进入／重新加载」，实际功能断言尚未执行。修复限于测试助手：识别正常／可恢复／致命三态，可恢复时点击真实继续按钮，再等待初始化和入场完成；致命失败抛出加载状态。生产加载语义与超时未调整。

新增 `test/e2e/boot.spec.js` 的两条真实浏览器回归：可选音乐失败后恢复菜单、核心模块失败显式报错。旧助手分别因等待超时及缺少真实错误失败，修复后 2/2 通过；完整本地 `npm test` 119/119、`npm run test:e2e` 12/12（2026-09-10 15:23:31 UTC 开始，0 flaky/0 skipped）。新 CI 结果以 PR 检查为准；原失败记录保留，不改写成通过。

输入审查复核：`Input.down` 保留已按住的键，`endFrame()` 只清边沿，后续重复按下不自动产生新边沿；普通持住 Z/X 失效的推断未得到行为验证。尝试在首次确认仅跳过演出后保持 Z，鼠标开局并显式派发 repeat，通过真实对话文本观察；原 guard 顺序下也通过，故撤下无区分力的临时测试，未改 `js/ui.js`，保留原有三条菜单输入回归。未添加第二个 Input、生产 hook 或原型替换，也不将敌弹调试数视为自机射击证据。

此次修复原始 CI 日志、原失败 JSON、新完整 E2E JSON 及文件哈希保存在本地 `../evidence/02-ci-recovery/`，未纳入提交。
